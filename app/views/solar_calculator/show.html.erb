<div class='container row'>

<div class="col s12 m6 welcome" id='left-card'>
  <div class="welcome-form">
    <%= form_tag('/calculator', :method => "get") do %>
    <h5 class="text-select">Select your State</h5>

      <div class="select state select-box">
        <%= select_tag('state', options_for_select(@us_states)) %>
      </div>

        <div class="col s12 m3 text zipcode">
          <%= text_field_tag(:zip, '', placeholder: 'zipcode') %>
        </div>

        <div class="col s12 m5 text avg-kwh">
          <%= text_field_tag(:kwh, nil, placeholder: 'Annual kilowatt hours') %>
        </div>

      <div class="select roof-angle select-box">
      <h5 id="text-select">Select roof pitch</h5>
        <%= select_tag('roof-angle', options_for_select([['10 degrees', '10'], ['14 degrees', '14'],
                                                        ['18 degrees', '18'], ['22 degrees', '22'],
                                                        ['26 degrees', '26'], ['30 degrees', '30'],
                                                        ['33 degrees', '33'], ['36 degrees', '36'],
                                                        ['40 degrees', '40'], ['42 degrees', '42'],
                                                        ['45 degrees', '45']])) %>
      </div>

      <h5 id="text-select azimuth-text">Select azimuth angle</h5>
      <div class="select azimuth-angle select-box">
        <%= select_tag('azimuth-angle', options_for_select([['180 degrees south', '180']])) %>
      </div>

      <h5 id="text-select system-size">Select system size</h5>
      <div class="select system-size select-box">
        <%= select_tag('system', options_for_select(@system_capacity)) %>
      </div>

      <div class'submit-button'>
        <button class='btn btn-lg btn-success btn-home green lighten-2' type='button' >Calculate</button>
      </div>

    <% end %>
  </div>
</div>

<div class='col s12 m6' id='right-card'>
  <div class='header'>
    <h4 id='result-header'>Results</h4>
  </div>
  <div class='results'>
    <h5 class='pv-output'></h5>
  </div>
  <div class='results'>
    <h5 class='percentage-offset'></h5>
  </div>
  <div class='results'>
    <h5 class='upfront-cost'></h5>
  </div>
  <div class='results'>
    <h5 class='years-to-payoff'></h5>
  </div>
</div>
</div>
